## What & why

<!-- What does this change, and why is it needed? -->

## Testing

<!-- How did you verify this? e.g. `npm test`, `npm run build`, manual steps -->

## Checklist

- [ ] `cd server && npm test` passes
- [ ] `cd web && npm run build` passes
- [ ] Migrations added if the schema changed (`server/migrations/000N_*.sql`)
